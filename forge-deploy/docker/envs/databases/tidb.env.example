# ------------------------------------------------------------------
# TiDB provider overrides. Activate only when DATABASE_TYPE=tidb.
# Wire-protocol compatible with MySQL; same charset rules apply.
# Copy as tidb.env.
# ------------------------------------------------------------------
DATABASE_PORT=4000
DATABASE_CHARSET=utf8mb4
DATABASE_SSL_MODE=disable
