# ------------------------------------------------------------------
# MySQL provider overrides. Activate only when DATABASE_TYPE=mysql.
# Copy as mysql.env.
# ------------------------------------------------------------------
DATABASE_PORT=3306
# Driver charset; utf8mb4 strongly recommended.
DATABASE_CHARSET=utf8mb4
# When using TLS, set to "preferred" / "required".
DATABASE_SSL_MODE=disable
