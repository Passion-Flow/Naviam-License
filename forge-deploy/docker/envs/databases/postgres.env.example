# ------------------------------------------------------------------
# PostgreSQL provider overrides. Activate only when DATABASE_TYPE=postgres
# in .env and you need to tweak driver-specific knobs.
# Copy as postgres.env.
# ------------------------------------------------------------------
DATABASE_PORT=5432
DATABASE_SSL_MODE=disable
# Comma-separated server-side options, e.g. "search_path=public,extra"
DATABASE_OPTIONS=
