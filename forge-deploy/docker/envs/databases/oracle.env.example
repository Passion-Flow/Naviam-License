# ------------------------------------------------------------------
# Oracle provider overrides. Activate only when DATABASE_TYPE=oracle.
# Copy as oracle.env.
# ------------------------------------------------------------------
DATABASE_PORT=1521
# Oracle service name (DATABASE_DATABASE is the schema/user name).
DATABASE_SERVICE_NAME=ORCLPDB1
# Wallet path (optional, for mTLS).
DATABASE_WALLET_PATH=
