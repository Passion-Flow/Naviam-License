# ------------------------------------------------------------------
# Optional: forge-scheduler only knobs. Copy as scheduler.env.
# ------------------------------------------------------------------
# Beat schedule storage backend ("disk" persists to a mounted volume;
# "memory" loses state on restart — fine for low-stakes intervals).
CELERY_BEAT_STORAGE=memory
