# ------------------------------------------------------------------
# nginx reverse proxy knobs. Copy as nginx.env.
# ------------------------------------------------------------------
# Max upload size — .forge files can be tens of MB.
NGINX_PROXY_BODY_SIZE=10m
# Server name (used in nginx conf.d templates).
NGINX_SERVER_NAME=forge.local
