from django.apps import AppConfig


class ActivationsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "modules.activations"
    label = "activations"
    verbose_name = "Activations"
