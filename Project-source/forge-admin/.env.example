# forge-admin — dev server 配置样例

# Vite dev server
VITE_DEV_PORT=13000
VITE_DEV_HOST=0.0.0.0
VITE_DEV_API_TARGET=http://localhost:13001

# 注意：生产 / 私有化部署 endpoint 不通过 build-time 注入；
# 而是通过容器入口脚本替换 index.html 中的 window.__APP_CONFIG__。
